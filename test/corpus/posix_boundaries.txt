==================
Empty text lines do not absorb following commands
==================
a\

p
i\

d
1,2c\

p

---

(script
  (command_list
    (command
      body: (append_command
        text: (text_block)))
    (separator)
    (command
      body: (print_command))
    (separator)
    (command
      body: (insert_command
        text: (text_block)))
    (separator)
    (command
      body: (delete_command))
    (separator)
    (command
      addresses: (address_range
        start: (address
          (line_number_address))
        end: (address
          (line_number_address)))
      body: (change_command
        text: (text_block)))
    (separator)
    (command
      body: (print_command))
    (separator)))

==================
Text continuation uses odd trailing backslash parity
==================
a\
two\\
p
a\
three\\\
continued
d

---

(script
  (command_list
    (command
      body: (append_command
        text: (text_block)))
    (separator)
    (command
      body: (print_command))
    (separator)
    (command
      body: (append_command
        text: (text_block)))
    (separator)
    (command
      body: (delete_command))
    (separator)))

==================
POSIX block brace whitespace
==================
{
  p
  }
{p;   }
{
    }
{ ; }

---

(script
  (command_list
    (command
      body: (block_command
        (command_list
          (separator)
          (command
            body: (print_command))
          (separator))))
    (separator)
    (command
      body: (block_command
        (command_list
          (command
            body: (print_command))
          (separator))))
    (separator)
    (command
      body: (block_command
        (command_list
          (separator))))
    (separator)
    (command
      body: (block_command
        (command_list
          (separator))))
    (separator)))

==================
Negated comments
==================
!# comment
p

---

(script
  (command_list
    negation: (negation)
    (comment_command)
    (separator)
    (command
      body: (print_command))
    (separator)))

==================
Command spacing
==================
1,2p
1 !p
b label
t label

---

(script
  (command_list
    (command
      addresses: (address_range
        start: (address
          (line_number_address))
        end: (address
          (line_number_address)))
      body: (print_command))
    (separator)
    (command
      addresses: (address
        (line_number_address))
      negation: (negation)
      body: (print_command))
    (separator)
    (command
      body: (branch_command
        label: (label)))
    (separator)
    (command
      body: (test_command
        label: (label)))
    (separator)))

==================
Invalid bare label requires a name
==================
:
p

---

(script
  (ERROR)
  (command_list
    (separator)
    (command
      body: (print_command))
    (separator)))

==================
GNU empty block
==================
{}
p

---

(script
  (command_list
    (command
      body: (block_command))
    (separator)
    (command
      body: (print_command))
    (separator)))

==================
Trailing blank before block newline
==================
{
  p 
}
p

---

(script
  (command_list
    (command
      body: (block_command
        (command_list
          (separator)
          (command
            body: (print_command))
          (separator))))
    (separator)
    (command
      body: (print_command))
    (separator)))

==================
Trailing blank before block semicolon
==================
{p ;}
p

---

(script
  (command_list
    (command
      body: (block_command
        (command_list
          (command
            body: (print_command))
          (separator))))
    (separator)
    (command
      body: (print_command))
    (separator)))

==================
GNU blank before address comma
==================
1 ,2p
p

---

(script
  (command_list
    (command
      addresses: (address_range
        start: (address
          (line_number_address))
        end: (address
          (line_number_address)))
      body: (print_command))
    (separator)
    (command
      body: (print_command))
    (separator)))

==================
GNU blank after address comma
==================
1, 2p
p

---

(script
  (command_list
    (command
      addresses: (address_range
        start: (address
          (line_number_address))
        end: (address
          (line_number_address)))
      body: (print_command))
    (separator)
    (command
      body: (print_command))
    (separator)))

==================
GNU blank after negation
==================
! p
p

---

(script
  (command_list
    (command
      negation: (negation)
      body: (print_command))
    (separator)
    (command
      body: (print_command))
    (separator)))

==================
GNU blank in negated comment
==================
! # comment
p

---

(script
  (command_list
    negation: (negation)
    (comment_command)
    (separator)
    (command
      body: (print_command))
    (separator)))

==================
GNU multiple spaces before branch label
==================
b  label
p

---

(script
  (command_list
    (command
      body: (branch_command
        label: (label)))
    (separator)
    (command
      body: (print_command))
    (separator)))

==================
GNU tab before branch label
==================
b	label
p

---

(script
  (command_list
    (command
      body: (branch_command
        label: (label)))
    (separator)
    (command
      body: (print_command))
    (separator)))

==================
GNU multiple spaces before test label
==================
t  label
p

---

(script
  (command_list
    (command
      body: (test_command
        label: (label)))
    (separator)
    (command
      body: (print_command))
    (separator)))

==================
GNU tab before test label
==================
t	label
p

---

(script
  (command_list
    (command
      body: (test_command
        label: (label)))
    (separator)
    (command
      body: (print_command))
    (separator)))

==================
GNU empty backslash text at EOF
==================
a\

---

(script
  (command_list
    (command
      body: (append_command
        text: (text_block)))))
